# HEOS Real Home Setup

1. Copy `config/real_home.example.json` to `config/real_home.json`.
2. Replace every `REPLACE_...` value with the exact Home Assistant entity ID.
3. Keep `"dry_run": true`.
4. Run only read-only collection first.

The first objective is not control. The first objective is trustworthy observation.
